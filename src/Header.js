import React from "react";
import styled from "styled-components";

const Logo = styled.div`
  font-family: "Major Mono Display", monospace;
  font-size: 40px;
  margin: 30px 0;
`;

export default () => (
  <>
      <Logo>SpotifY MemEgeN</Logo>
  </>
);
